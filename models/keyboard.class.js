/**
 * Represents the state of the keyboard keys used in the game.
 * Stores the state of each key as a boolean value.
 */
class Keyboard {
    LEFT = false;
    RIGHT = false;
    UP = false;
    DOWN = false;
    SPACE = false;
    D = false;
    F = false;

    /**
     * Constructs a Keyboard object.
     */
    constructor() {
        // Initializes the state of each key to false.
    }
}
